# Prompt: Wrap "بطاقات مراجعة القرآن" as a native app with Capacitor

أنا عندي موقع ويب اسمه "بطاقات مراجعة القرآن" — تطبيق مراجعة قرآن بالبطاقات التعليمية. بدي تساعدني أغلّفه بـCapacitor لصيغة تطبيق iOS وAndroid حقيقي يُنشر بمتاجر التطبيقات، بدون ما نعيد كتابة أي منطق — نفس الكود بالضبط، بس متغلّف كتطبيق أصلي.

## السياق التقني

- تطبيق ويب ثابت (static): HTML/CSS/JS عادي، بدون build step، بدون framework.
- الملفات الأساسية: `index.html` (كل الشاشات بملف واحد)، `app.js`، `auth-ui.js`، `supabase-client.js`، `quran-reader.js`، `styles.css`، `config.js`.
- الخلفية: Supabase (auth + database + Realtime + Edge Functions) — يتحمّل من CDN بـ`<script>` tag بالـ`index.html`، مع fallback لوضع الضيف (guest mode) لو فشل التحميل.
- بيانات القرآن (نص المصحف، تخطيط الصفحات) محلية بالكامل (ملفات JSON بالمشروع) — ما بتحتاج إنترنت.
- الصوت (تلاوة) بيجي من everyayah.com كملفات mp3 مباشرة (رابط ثابت لكل قارئ/آية) — بيحتاج إنترنت.
- فيه تسجيل دخول (Supabase Auth)، وضع ضيف كامل الوظائف بدون حساب، ومزامنة XP/إنجازات/جلسات للمسجلين.
- الواجهة أصلاً مبنية بعقلية موبايل: bottom nav ثابت تحت، swipe للتنقل بصفحات القرآن، تصميم متجاوب.

## المطلوب

1. غلّف المشروع بـCapacitor (iOS + Android) مع أقل تعديل ممكن على الكود الحالي — نفس ملفات HTML/CSS/JS تضل هي المصدر، Capacitor بس يوفر الـshell الأصلي حوالها.
2. تعامل بعناية مع النقاط التالية الخاصة بهالمشروع تحديدًا:
   - **مكتبة Supabase**: حاليًا محمّلة من CDN خارجي. افحص هل الأفضل نستضيفها محليًا (vendor) بدل الاعتماد على الإنترنت لتحميل المكتبة نفسها كل مرة بتطبيق منشور بالمتجر، وقترح الحل الأنسب.
   - **تسجيل الدخول**: إذا في أي OAuth أو magic link بتدفق تسجيل الدخول، لازم نتأكد من التعامل الصحيح مع الـredirect جوا WebView (custom URL scheme / Capacitor Browser plugin عند الحاجة).
   - **الشبكة المسموحة**: إعداد `capacitor.config` لتسمح بالاتصال بدومين Supabase الخاص فيني وeveryayah.com (`server.allowNavigation` أو ما يعادلها).
   - **زر الرجوع (Android)**: لازم يتم ربطه بالتنقل الداخلي للتطبيق (بين التبويبات/صفحات القرآن) بدل ما يقفل التطبيق مباشرة.
   - **إيماءة الرجوع بحافة الشاشة (iOS/Android)**: تأكد ما في تعارض مع الـswipe الموجود أصلاً بقارئ القرآن (سحب يمين/شمال للتنقل بين الصفحات).
   - **Safe area / notch**: عدّل الـCSS إذا لزم عشان الـtopbar والـbottom nav يحترموا مساحات الأمان (خصوصًا إنو شاشة القرآن مصممة كـ"fixed viewport").
   - **splash screen وأيقونة التطبيق**: جهّز الإعداد الأساسي (حتى لو بأصول placeholder مؤقتًا).
   - **وضع الضيف**: تأكد إنو localStorage شغّال طبيعي جوا الـWebView (ما في داعي لتغييره لـCapacitor Preferences إلا إذا في سبب واضح).
3. جهّز تعليمات تشغيل واضحة: `npx cap init`، إضافة المنصتين، `npx cap sync`، وكيف نفتح المشروع بـXcode/Android Studio للتجربة.

## طريقة العمل يلي بفضلها

- ناقش الخطة والقرارات التقنية (خصوصًا نقطة استضافة مكتبة Supabase وطريقة التعامل مع تسجيل الدخول) قبل ما تبلش تنفذ أي شي.
- لو التغيير كبير، قسمه لخطوات وأكد كل خطوة لحالها قبل ما تنتقل للتالية.
- سلّمني نسخ كاملة من أي ملف تعدّله (drop-in replacement) مع شرح مختصر ليش.
- لا تعيد كتابة منطق موجود إلا إذا كان لازم فعلاً لتوافق Capacitor.
